/**
 * WallType enum regroup the 3 differents type of walls available on Habbo.
 *
 * @enum
 * @memberof Scuti
 */
export enum WallType {
    CORNER_WALL,
    LEFT_WALL,
    RIGHT_WALL,
    DOOR_WALL
}
