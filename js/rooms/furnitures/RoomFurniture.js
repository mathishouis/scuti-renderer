export default class RoomFurniture {
    constructor(id, position, direction, state) {
        this.id = id;
        this.position = position;
        this.direction = direction;
        this.state = state;
    }
}